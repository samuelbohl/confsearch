
const Logo = () => {

    return (
        <div style={{ display: "flex", gap: "1rem", alignItems: "center", justifyContent: "center", fontSize: "25px", fontWeight: "bold" }}>
            <span>Computer Science</span>
            <span style={{color: "var(--main_color)"}}>Conference Search</span>
        </div>
    );
}

export default Logo;